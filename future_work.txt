Now, when creating new partners, I also want to include 2 fields - local loading cost & hub loading cost. The idea is that there are 2 types of loading, either they are loaded locally or they are loaded directly from hubs. So the local loading costs are fixed, while there is maximum hub loading costs. Include the 2 new fields in the partner header description. Now coming to dispatch planner, there are 2 types of dispatch methods - door delivery or go-down delivery. The current working system follows the door delivery, i.e. include the company's last mile coverage via company cards, while in go-down delivery, we are only delivering till the city, i.e. no need to enter pin-codes or even render the partner cards. So here's what we are going to do: 1) THe column count should go from 2 to 3, in the first is Width, Depth, Height, second row consists of invoice value, City & diesel, third row should be 3 inputs with 2 switches. first switch will have one of the two options: Local Loading or Hub Loading, if hub loading then render numeric input field too for calculation, second switch will have the option for door delivery or Godown delivery, render partners only if there is door delivery, else let the partner distances' be 0(for no calculation). Third field will be loading charges cost which is pre-defined by the text "ask from Sachin sir". If possible, make the third row's "header" like the same text, please do this for me