services:

  web:
    build: .
    image: tempo-erp-app:v1.1.1
    command: uvicorn server:app --host 0.0.0.0 --port 8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env